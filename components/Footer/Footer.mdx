{/* Footer.mdx */}

import {
  Canvas,
  Meta,
  Primary,
  Controls,
  Story,
  Stories,
} from "@storybook/blocks";

import * as Footer from "./Footer.stories";

<Meta of={Footer} />

# Footer

The nav bar is the bar at the top of every page. It holds a logo and either a set of links for a user that is not logged in, or the user name and a dropdown for users that are logged in.

If the screen is small, the menu will shrink to a hamburger dropdown.

<Primary />
<Controls />
<Stories />
