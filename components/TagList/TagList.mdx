{/* TagList.mdx */}

import {
  Canvas,
  Meta,
  Primary,
  Controls,
  Story,
  Stories,
} from "@storybook/blocks";

import * as TagList from "./TagList.stories";

<Meta of={TagList} />

# TagList

The top of every tag page shows a list of child tags for that parent. This is the component that does that. For each tag returned as a child, it displays the tag component as well as the description for that tag.

<Primary />
<Controls />
<Stories />
